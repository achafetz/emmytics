% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mixpanel.R
\name{convert_timestamp_local}
\alias{convert_timestamp_local}
\title{Convert UTC Timestamps to State-Specific Local Timezones}
\usage{
convert_timestamp_local(df)
}
\arguments{
\item{df}{A \code{data.frame} or \code{tibble} containing at minimum:
\describe{
\item{\code{pilot_state}}{A character column of two-letter U.S. state
abbreviations (e.g., \code{"LA"}, \code{"AZ"}, \code{"NH"}).}
\item{\code{timestamp}}{A \code{POSIXct} datetime column in UTC.}
}}
}
\value{
A \code{tibble} identical to \code{df} with the \code{timestamp} column converted
to each row's corresponding local timezone.
}
\description{
Converts a UTC \code{timestamp} column in a dataframe to the appropriate local
timezone based on a two-letter state abbreviation column (\code{pilot_state}).
Uses a built-in IANA timezone lookup table that can be extended as new
states are added to the \code{pilot_timezone} dataset.
}
\details{
Timezone mappings use IANA timezone strings (e.g., \code{"America/Chicago"}).
To add support for additional states, append rows to the internal
\code{pilot_timezone} tribble. A full list of valid IANA timezone strings can
be retrieved in R via \code{OlsonNames()}.

Note that Arizona (\code{"AZ"}) uses \code{"America/Phoenix"}, which does \strong{not}
observe Daylight Saving Time (DST), unlike most of the Mountain Time zone.
}
\note{
This function uses \code{lubridate::with_tz()} to convert the instant in
time to its local representation without altering the underlying moment.
}
