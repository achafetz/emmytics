% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mixpanel.R
\name{extract_properties}
\alias{extract_properties}
\title{Extract Properties from Nested Column}
\usage{
extract_properties(df, ...)
}
\arguments{
\item{df}{A data frame or tibble containing a nested \code{properties} column.}

\item{...}{<\code{\link[rlang:dyn-dots]{dynamic-dots}}> Additional column specifications
to extract from the \code{properties} column. These should be provided as
name-value pairs where the name is the new column name and the value is
the expression to extract (e.g., \code{user_id = properties$user_id}).}
}
\value{
A data frame with the same structure as \code{df} but with additional
columns extracted from the \code{properties} nested column.
}
\description{
Extracts key columns from a nested \code{properties} column in a data frame and
creates new columns at the top level. The function always extracts a core set
of properties and allows for flexible addition of extra columns through the
ellipsis (\code{...}) parameter.
}
\details{
The function first extracts a predefined set of core columns from the nested
\code{properties} column. The \code{help_topic} column is conditionally set to
\code{help_section} if \code{help_section} is not NA, otherwise it retains the value
from \code{properties$topic}.

Additional columns can be specified using the \code{...} parameter, allowing for
flexible extraction of other properties without modifying the function.
}
\examples{
\dontrun{
# Basic usage with core columns only
df_processed <- extract_properties(df_import)

# With additional columns
df_processed <- extract_properties(
  df_import,
  user_id = properties$user_id,
  timestamp = properties$timestamp,
  session_duration = properties$session_duration
)
}


}
